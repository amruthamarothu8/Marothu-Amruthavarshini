#include <stdio.h>

int main() {
    int a, b, sum;
    printf("25331A05I1\n");
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum = %d", sum);

    return 0;
}

